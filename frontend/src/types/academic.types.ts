export interface AcademicTerm {
  id: string
  name: string
  startDate: string
  endDate: string
  isActive: boolean
}

export interface DepartmentProgram {
  id: string
  name: string
  code: string
}
